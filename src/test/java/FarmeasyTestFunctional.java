import cl.farmeasy.model.Usuario;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertEquals;

/**
 * Clase para pruebas funcionales del sistema FarmEasy.
 * Estas pruebas estan destinadas a verificar flujos de extremo a extremo,
 * como el proceso de registro y login de un usuario.
 */
class FarmeasyTestFunctional {

    /**
     * Prueba el flujo completo de registro y login de un usuario.
     * Este es un placeholder para una prueba funcional mas completa.
     */
    @Test
    void TestProcesoRegistroYLogIn(){
        Usuario user = new Usuario();
    }
}